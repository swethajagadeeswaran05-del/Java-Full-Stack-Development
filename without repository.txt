//Dedicated class for data  access-[store and retrieve]
// why - clean and efficient code/later data part can be changed to dedicated database
so,
without repository
@RestController
@RequestMapping("/students")
public class studentController{
private List <student> students = new ArrayList<>();

@GetMapping
public List<student> getstudents(){
return students;
}
@PostMapping 
public void addstudent(@RequestBody student s){
students.add(S);
}
}
with repository 
1. model class
Public class Student{
private long id;
private string name;

public student(Long id,String name){
this.id=id;
this.name=name;
}
public Long getId(){
return id;}
public Long getName(){
return name;
}
}
